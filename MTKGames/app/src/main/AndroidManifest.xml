<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cat.paucasesnoves.mtkgames" >

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_app"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_appre"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true" >
        <activity
            android:name=".ui.iniciar.LoginAutomatico"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
        <activity android:name=".ui.iniciar.IniciarFragment" />
        <activity android:name=".ui.registro.RegistroFragment" />
        <activity android:name=".ui.tienda.TiendaFragment" />
        <activity
            android:name=".MainLogged"
            android:label="@string/title_activity_main_logged"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.MonederoFragment" />
        <activity android:name=".ui.BibliotecaFragment" />
        <activity android:name=".ui.ModificarFragment" />
        <activity android:name=".ui.tienda.JuegoActivity" />
        <activity android:name=".ui.CategoriaActivity" />
    </application>

</manifest>